const VideoPage = () => {
  return (
    <>
      {/* <video key={video.name} controls className="w-full rounded">
        <source src={video.url} />
        <track
          kind="subtitles"
          src={video.url}
          srcLang="en"
          label="English"
          default
        />
      </video> */}
    </>
  )
}

export default VideoPage